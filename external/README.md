# External references

No third-party repository is vendored in this bootstrap.

Run `scripts/fetch-references.ps1` on a development machine when local reference source is useful. The script clones public repositories into this directory; each repository retains its own license.
